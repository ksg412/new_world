<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sg.source.function.main.mapper.MainMapper">
    <select id="test" resultType="String">
        select count(*) from A_CM_A0010
    </select>

    <select id="getTbMapList" resultType="HashMap">
        select
            *
        from tb_map
        WHERE iId = 1
    </select>
</mapper>